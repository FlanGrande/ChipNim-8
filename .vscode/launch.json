// .vscode/launch.json
{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "nim: debug main",
            "program": "${workspaceFolder}/bin/ChipNim8",
            "preLaunchTask": "nim: build (for debugging)",
            "args": [],
            "cwd": "${workspaceFolder}",
            "breakpointMode": "file"
        }
    ]
}